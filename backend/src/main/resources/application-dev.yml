# 开发环境覆盖（可选）
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/search_engine?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ''
  data:
    redis:
      host: localhost
      port: 6379
      password: ''

logging:
  level:
    com.searchengine: DEBUG
